import React, { useState } from 'react';

const YantraHomepage = () => {
  const [activeTab, setActiveTab] = useState('Home');

  const menuItems = ['Home', 'About', 'Menu', 'Press', 'Reservations', 'The Grand Trunk', 'e-Vouchers', 'Contact Us'];

  return (
    <div className="h-screen flex flex-col" style={{
      backgroundImage: "url('2.png')",
      backgroundSize: 'cover',
      backgroundPosition: 'center'
    }}>
      {/* Header */}
      <header className="bg-black bg-opacity-50 text-white p-4">
        <div className="container mx-auto flex justify-between items-center">
          <div className="text-2xl font-bold">YANTRA</div>
          <nav>
            <ul className="flex space-x-4">
              {menuItems.map((item) => (
                <li key={item}>
                  <button
                    className={`hover:underline ${activeTab === item ? 'underline' : ''}`}
                    onClick={() => setActiveTab(item)}
                  >
                    {item}
                  </button>
                </li>
              ))}
            </ul>
          </nav>
        </div>
      </header>

      {/* Main Content */}
      <main className="flex-grow flex items-center justify-center">
        <div className="text-center text-white">
          <h1 className="text-5xl font-bold mb-4 shadow-text">Welcome to Yantra</h1>
          <p className="text-xl mb-8 shadow-text">Embark on a culinary journey</p>
          <button className="bg-white text-black px-6 py-2 rounded-full hover:bg-gray-200 transition duration-300">
            Explore Our Menu
          </button>
        </div>
      </main>

      {/* Footer */}
      <footer className="bg-black bg-opacity-50 text-white p-4">
        <div className="container mx-auto flex justify-between text-sm">
          <span>© Yantra (S) Pte. Ltd. 2023</span>
          <span>Terms of Service and Privacy Policy</span>
        </div>
      </footer>

      <style>{`
        .shadow-text {
          text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }
      `}</style>
    </div>
  );
};

export default YantraHomepage;