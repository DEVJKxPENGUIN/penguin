<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yantra Landing Page</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Arial, sans-serif;
        }
        .container {
            background-image: url('/api/placeholder/1920/1080');
            background-size: cover;
            background-position: center;
            height: 100vh;
            position: relative;
        }
        .header {
            display: flex;
            justify-content: space-between;
            padding: 20px;
            color: white;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
        }
        .menu {
            display: flex;
        }
        .menu-item {
            margin-left: 20px;
            cursor: pointer;
        }
        .center-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
        }
        .explore-button {
            padding: 10px 20px;
            font-size: 18px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .message {
            margin-top: 20px;
            padding: 10px;
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">YANTRA</div>
            <div class="menu">
                <div class="menu-item" onclick="handleMenuClick('Home')">Home</div>
                <div class="menu-item" onclick="handleMenuClick('About')">About</div>
                <div class="menu-item" onclick="handleMenuClick('Menu')">Menu</div>
                <div class="menu-item" onclick="handleMenuClick('Press')">Press</div>
                <div class="menu-item" onclick="handleMenuClick('Reservations')">Reservations</div>
                <div class="menu-item" onclick="handleMenuClick('The Grand Trunk')">The Grand Trunk</div>
                <div class="menu-item" onclick="handleMenuClick('e-Vouchers')">e-Vouchers</div>
                <div class="menu-item" onclick="handleMenuClick('Contact Us')">Contact Us</div>
            </div>
        </div>
        <div class="center-content">
            <button class="explore-button" onclick="handleExploreClick()">Explore Yantra</button>
            <div id="message" class="message" style="display: none;"></div>
        </div>
    </div>

    <script>
        function handleMenuClick(item) {
            showMessage(`${item} clicked!`);
        }

        function handleExploreClick() {
            showMessage('Explore Yantra clicked!');
        }

        function showMessage(text) {
            const messageElement = document.getElementById('message');
            messageElement.textContent = text;
            messageElement.style.display = 'block';
            setTimeout(() => {
                messageElement.style.display = 'none';
            }, 3000);
        }
    </script>
</body>
</html>